apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-ingress
spec:
  rules:
    - http:
        paths:
          - path: /Home
            pathType: Prefix
            backend:
              service:
                name: service1-service
                port:
                  number: 80
          - path: /Snus
            pathType: Prefix
            backend:
              service:
                name: service2-service
                port:
                  number: 80
          